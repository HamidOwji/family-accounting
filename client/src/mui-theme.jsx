import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
      primary: {
        main: '#2DBD88', // The primary main color
      },
      secondary: {
        main: '#004561', // The secondary main color
      },
      background: {
        default: '#FFFFD9', // The default background color
      },
      text: {
        primary: '#212427', // The primary text color
        secondary: '#FFFFFF', // The secondary text color
      },
    },
  });

export default theme;

